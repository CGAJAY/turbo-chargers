"use client";

import { Products } from "@/components/products";

const Home = () => {
    return (
        <main className="p-6 max-w-7xl mx-auto">
            <Products />
        </main>
    );
};

export default Home;
